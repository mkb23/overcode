# Reset to main branch

Run these commands to reset to main branch with latest changes:

```bash
git checkout main && git pull origin main
```

After completing this, tell the user: "Done. Run `/clear` to clear context."
