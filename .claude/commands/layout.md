Switch the demo tmux session to the "{{layout_name}}" layout using the layouts.sh script on the overcode tmux server.

Available layouts: logs, default, grid, sidebyside, stacked

If the layout name is not provided or invalid, list the available layouts instead.

Make sure to use TMUX_CMD="tmux -L overcode" when calling layouts.sh.
